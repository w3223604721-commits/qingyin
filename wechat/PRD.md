# 轻印（旅行记忆）产品需求文档

> 版本：内测 2.1 (Beta 2.1)  
> 更新日期：2026-06-09  
> 产品定位：轻量级旅行日记与足迹记录小程序

---

## 1. 产品概述

### 1.1 产品简介

轻印（Map + Memory）是一款面向旅行爱好者的轻量级旅行记录工具。用户可以通过地图打卡、日记撰写、照片管理等功能，将每一次旅行的足迹和记忆完整保留下来。产品核心理念是"在地图上留下脚印，用照片和文字珍藏记忆"——做减法，拒绝功能堆砌，只保留最核心的旅行记录能力。

### 1.2 目标用户

- 喜欢旅行并希望记录旅行足迹的年轻用户（20-40 岁）
- 对打卡记录、旅行日记有需求的自由行旅客
- 希望用简洁工具替代复杂旅行 App 的轻度用户

### 1.3 产品价值主张

- **简洁直观**：不做复杂功能堆砌，核心功能一目了然
- **地图驱动**：以地图为基底，让足迹可视化
- **记忆珍藏**：照片 + 文字 + 地理位置的完整记忆闭环
- **轻量分享**：支持小票风格等创意分享模板

### 1.4 技术栈

- 前端：原生 HTML/CSS/JavaScript（单页应用 SPA）— `wechat/index.html` 为唯一入口
- 后端认证：Cloudflare Workers + D1 SQL Database
- 地图：Leaflet + 天地图瓦片
- 数据存储：浏览器 LocalStorage（本地存储）+ Cloudflare D1（用户账号）
- 图片导出：html2canvas
- 运行环境：微信小程序 / 移动端 H5
- 部署：Vercel（主）+ CloudBase 静态托管（镜像）

---

## 2. 功能架构

### 2.1 整体结构

```
轻印
│   ├── 旅程列表
│   ├── 旅程详情（图片轮播 + 日记列表）
│   └── 新建/编辑旅程
├── Tab 2：打卡（Check-in）
│   ├── 全屏地图（天地图）
│   ├── 打卡点管理
│   ├── 足迹轨迹记录与回放
│   └── 地图工具栏
├── Tab 3：分享（Share）
│   ├── 日志导出（图片）
│   └── 自定义模板（小票风格等）
├── Tab 4：我的（Profile）
│   ├── 个人资料与头像
│   ├── 勋章系统（34 省市区）
│   ├── 旅行人格
│   ├── 数据洞察（月度活跃/足迹增长/季节分布/标签统计）
│   └── 设置（账号安全/回收站/权限管理/反馈/关于）
└── 全局
    ├── 权限管理系统（通知/位置/照片/相机）
    ├── FAB 浮动操作按钮
    └── 底部导航栏
```

### 2.2 信息架构图

```
首页（日志 Tab）
  │
  ├── 旅程列表
  │     └── 点击进入旅程详情
  │           ├── 图片轮播（记忆点）
  │           ├── 日记列表
  │           │     └── 点击进入日记详情（可编辑/删图）
  │           └── 添加新日记
  │
  ├── 打卡 Tab
  │     ├── 地图视图（全屏）
  │     ├── 添加打卡（选位置 + 照片 + 备注）
  │     └── 足迹记录/回放
  │
  ├── 分享 Tab
  │     ├── 日志导出（选旅程 → 导出图片）
  │     └── 自定义模板（小票风格）
  │
  └── 我的 Tab
        ├── 个人资料
        ├── 勋章
        ├── 旅行人格
        ├── 数据洞察
        └── 设置 → 权限管理/回收站/反馈/关于
```

---

## 3. 功能详细说明

### 3.1 日志模块（Tab 1）

#### 3.1.1 旅程列表

| 属性 | 说明 |
|------|------|
| 触发 | 进入日志 Tab 自动加载 |
| 展示 | 旅程卡片列表，每张卡片含名称、城市、日期范围、简介、封面图 |
| 交互 | 点击卡片进入旅程详情；长按/菜单按钮弹出上下文菜单（编辑/删除） |
| 空状态 | 无旅程时展示引导文案 + 空状态插画 |

**旅程数据模型：**
```
{
  id: string,
  name: string,          // 旅程名称（必填，≤30字）
  city: string,          // 目的地城市（≤50字）
  startDate: string,     // 开始日期 YYYY-MM-DD
  endDate: string,       // 结束日期
  desc: string,          // 简介（≤200字）
  coverPhoto: string,    // 封面图片 Base64
  days: [],              // 日记条目数组
  createdAt: string,     // 创建时间 ISO
  tags: []               // 标签
}
```

#### 3.1.2 旅程详情

- **图片轮播**：从所有日记中提取照片，自动形成轮播（记忆点功能），支持左右滑动切换、指示点导航、计数器显示
- **日记列表**：按时间排序展示所有日记卡片，每张卡片含日期、标题、内容、照片预览
- **添加日记**：底部按钮，打开日记编辑模态框

#### 3.1.3 日记编辑

| 属性 | 说明 |
|------|------|
| 字段 | 日期、标题（≤30字）、内容（≤500字）、照片（最多50张） |
| 图片 | 支持拍摄/选取图片，带权限控制 |
| 编辑 | 可修改已有日记所有字段，支持删除日记 |

### 3.2 打卡模块（Tab 2）

#### 3.2.1 地图视图

- 使用天地图瓦片作为底图（Key: `b467feef6280075e4d11fad1ea502609`）
- 全屏展示，适配移动端
- 已打卡位置以标记点展示在地图上
- 支持缩放、拖拽等标准地图操作

#### 3.2.2 打卡操作

| 属性 | 说明 |
|------|------|
| 触发 | 地图右侧工具栏 "+" 按钮 |
| 字段 | 地点名称（必填）、城市、省份、照片、备注 |
| 定位 | 支持使用当前位置自动填充 |
| 图片 | 支持拍摄/选取，带权限控制 |

**打卡数据模型：**
```
{
  id: string,
  place: string,         // 地点名称（必填，≤30字）
  city: string,          // 城市
  province: string,      // 省份
  lat: number,           // 纬度
  lng: number,           // 经度
  photo: string,         // 照片 Base64
  note: string,          // 备注（≤200字）
  createdAt: string
}
```

#### 3.2.3 足迹记录

- **开始记录**：点击"开始记录"按钮后，持续追踪用户位置
- **足迹点**：定时记录 GPS 坐标，形成轨迹
- **停止记录**：再次点击停止，保存完整轨迹
- **回放功能**：左侧工具栏播放按钮，按时间顺序在地图上回放轨迹

### 3.3 分享模块（Tab 3）

#### 3.3.1 日志导出

- 从已有旅程列表中选择要导出的旅程
- 生成包含旅程信息的图片（封面图 + 日记摘要 + 照片）
- 支持保存到相册

#### 3.3.2 自定义模板（小票风格）

- **风格选择**：小票风格（已上线）/ 卡片风格（即将上线）/ 时间线（即将上线）/ 杂志风（即将上线）
- **自定义内容**：分享标题、选择 2-9 张图片、每张图片可配文字/地点/日期
- **预览与导出**：生成小票样式预览图，支持分享到微信/QQ 或保存相册

### 3.4 我的模块（Tab 4）

#### 3.4.1 个人资料

- 头像（支持拍摄/选取上传）
- 昵称（≤15字）
- 个性签名（≤50字）
- 旅行统计（旅程数、打卡数）

#### 3.4.2 勋章系统

- 基于中国 34 个省级行政区的打卡勋章
- 到达一个省份即点亮对应勋章
- 支持按"全部/已点亮/未点亮"筛选
- 展示点亮进度和完成百分比

#### 3.4.3 旅行人格

- 根据用户旅行数据自动判断旅行人格类型
- 如：探索者、打卡达人、城市漫游者等

#### 3.4.4 数据洞察

| 洞察维度 | 说明 |
|----------|------|
| 月度活跃 | 近 12 个月旅行活跃度柱状图 |
| 足迹增长 | 近 12 天足迹点统计折线图 |
| 季节分布 | 四季旅行偏好环形图 |
| 标签统计 | 标签使用频率统计 |

#### 3.4.5 设置

- **账号与安全**：展示账号类型（本地账户）、数据存储方式、设备 ID、创建时间
- **行程回收站**：恢复已删除的旅程（支持恢复和永久删除）
- **权限管理**：查看和重置通知/位置/照片/相机四项权限
- **开发者反馈**：问题反馈和功能建议提交表单
- **版本信息**：内测 2.0 Build 2026.06
- **清除所有数据**：一键清空（带确认，不可恢复）

---

## 4. 权限管理系统

### 4.1 权限类型

| 权限 | 触发时机 | 选项 |
|------|----------|------|
| 通知 | 首次进入应用 | 允许 / 不允许 |
| 位置 | 通知权限选择后 | 永不 / 下次询问 / 使用 App 期间 / 始终 |
| 照片图库 | 首次选取图片 | 不允许 / 受限访问 / 完全访问 |
| 相机 | 首次拍摄照片 | 允许 / 不允许 |

### 4.2 权限记忆机制

- 所有权限选择存储在 `localStorage` 中（Key: `qingyin_permissions`）
- 用户做出选择后，后续不再重复询问
- 可在设置 → 权限管理中查看当前权限状态并重置单项权限
- 重置后，下次使用对应功能时将重新询问

### 4.3 图片添加流程

所有图片添加入口统一改为"拍摄/选取图片"二选一模式：

```
用户点击"添加图片"
  → 弹出 ActionSheet：📸 拍摄 / 🖼️ 选取图片 / 取消
    → 拍摄：检查相机权限 → 打开相机
    → 选取图片：检查照片权限 → 打开相册
```

涉及位置：旅程封面、日记照片、日记详情照片、打卡照片、分享模板图片、头像上传

---

## 5. 非功能需求

### 5.1 性能要求

- 页面首次加载时间 ≤ 3 秒
- 图片列表渲染不卡顿（支持虚拟滚动）
- 地图加载与交互流畅（60fps）
- LocalStorage 读写操作不影响 UI 响应

### 5.2 兼容性

- 微信小程序 WebView 环境
- iOS Safari 14+
- Android Chrome 90+
- 移动端 H5 浏览器

### 5.3 数据安全

- 所有数据存储在用户设备本地（LocalStorage）
- 不涉及云端同步（当前版本）
- 提供导出/导入数据功能作为备份手段
- 删除数据有确认弹窗保护

### 5.4 UI/UX 规范

- 最大内容宽度 500px，移动端优先设计
- 底部导航栏高度 64px
- 主题色：`#6366F1`（靛蓝紫）
- 圆角体系：8px / 12px / 16px / 20px
- 字体：系统默认中文字体栈（PingFang SC / Microsoft YaHei）

---

## 6. 数据流与存储

### 6.1 数据结构

```javascript
{
  journeys: [Journey],      // 旅程数组
  checkins: [Checkin],      // 打卡数组
  footprintTracks: [],      // 足迹轨迹数组
  profile: {                // 用户资料
    name: string,
    bio: string,
    avatar: string|null
  },
  currentJourneyId: string|null,  // 当前浏览的旅程
  trash: [Journey],         // 回收站
  carouselIndex: {}         // 轮播位置记忆
}
```

### 6.2 存储方式

- 主数据：`localStorage` → Key `qingyin_data`
- 权限数据：`localStorage` → Key `qingyin_permissions`
- 图片：Base64 编码存储在主数据中

### 6.3 数据流转

```
用户操作 → 更新内存数据(appData) → 持久化到 localStorage → 刷新 UI
```

---

## 7. 版本规划

### 内测 2.1（当前版本 · 2026-06-09）

> ⚠️ **内测 2.1 说明**：登录系统从 CloudBase 迁移至自建 Cloudflare Workers 后端，实现真正的账号密码注册/登录功能，不再依赖第三方登录 SDK。同时确认 `wechat/` 纯 HTML/CSS/JS 版本为主产品，废弃 Vue SPA 迁移。

#### 🆕 新增功能

- [x] **Cloudflare D1 数据库**：新建 `qingyin-db`，创建 Users 表（id, username, password_hash, nickname, avatar_url, security_question, security_answer, created_at, updated_at）
- [x] **Cloudflare Workers API 后端**：部署 `qingyin-api` Worker，提供 6 个 RESTful 接口（注册/登录/鉴权/忘记密码查询/重置密码/探索数据）
- [x] **用户注册**：支持用户名 + 密码注册，密码 SHA-256 哈希存储
- [x] **用户登录**：账号密码登录，返回 JWT-like token（7天有效期），错误时统一提示"账号或密码错误"
- [x] **Token 鉴权**：`/api/me` 接口验证 token 并返回用户信息
- [x] **密保系统**：注册时必选密保问题（8个预设选项）+ 答案，答案加密存储
- [x] **忘记密码**：输入用户名 → 系统返回注册时的密保问题 → 回答密保 → 验证通过后设置新密码（旧密码清除）→ 返回登录
- [x] **协议页面内置**：用户协议和隐私声明内嵌在应用中，z-index 10000，高于登录层
- [x] **登录覆盖层 fixed 定位**：三重保险（HTML 内联 style + CSS !important + JS style.setProperty）

#### 🎨 UI 改版

- [x] **登录页面**：Indigo 品牌色（#6366F1），Logo + 表单 + 协议勾选，无缝切换登录/注册/忘记密码
- [x] **密码显示切换**：眼睛图标切换明文/密文显示
- [x] **注册表单验证**：用户名5-24位仅字母数字下划线，密码至少6位
- [x] **表单间距统一**：登录表单 gap 24px，注册表单 gap 28px，忘记密码表单 gap 24px

#### 🐛 修复的 Bug

- [x] **switchLoginView 破坏 flex 布局**：`display:block` 覆盖 CSS `display:flex` 导致表单 gap 间距消失 → 改为 `display:''` 移除内联覆盖，让 CSS flex 生效
- [x] **从注册/忘记密码切回登录后间距消失**：所有视图切换统一使用 `display:''` 释放 flex 布局
- [x] **注册页面删除手机号字段**：HTML/JS/Worker 三端同步移除 phone 参数
- [x] **协议覆盖层 z-index 不足**：协议层 z-index 提升至 10000，高于登录层的 99999
- [x] **忘记密码"账号不存在"提示**：Worker 返回明确的"账号不存在"，前端红色高亮显示
- [x] **密保答案错误处理**：前端提示"密保答案错误，请重新输入"并清空输入框

#### 🔧 部署架构变更

| 项目 | 变更前 | 变更后 |
|------|--------|--------|
| 主产品 | Vue 3 SPA (src/) | 纯 HTML/CSS/JS SPA (wechat/) |
| 登录方式 | CloudBase 匿名登录 | Cloudflare Workers JWT Token |
| Vercel 部署 | `npm run build` (Vue) | 静态文件直出 |
| .gitignore | wechat/ 被忽略 | wechat/ 已纳入版本管理 |
| 注册字段 | 用户名 + 手机号 + 密码 | 用户名 + 密码 + 密保问题 |
| API 接口数 | 4 个 | 11 个（注册/登录/鉴权/忘记密码/重置密码/探索 + 管理后台5个端点） |

#### 🔗 部署信息（更新）

- [x] **Vercel 前端部署**：`https://20260603112143.vercel.app`（自动部署）
- [x] **Vercel 管理后台**：`https://20260603112143.vercel.app/admin/`
- [x] **CloudBase 镜像部署**：`https://ai-native-d5gv1bzqle900971e-1439954016.tcloudbaseapp.com/`
- [x] **CloudBase 管理后台**：`https://ai-native-d5gv1bzqle900971e-1439954016.tcloudbaseapp.com/admin/`
- [x] **Worker 后端地址**：`https://qingyin-api.w3223604721.workers.dev`
- [x] **管理员默认密码**：`qingyin2024admin`（可在 Cloudflare Workers 环境变量 `ADMIN_PASSWORD` 中修改）

#### 📁 修改文件（2026-06-09）

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `wechat/index.html` | 修改 | 删除手机号输入框、优化忘记密码视图 HTML、CSS 版本号 v7 |
| `wechat/script.js` | 修改 | 修复 switchLoginView flex 布局 bug、doRegister 移除 phone、doLogin 统一错误提示、优化忘记密码流程 |
| `wechat/styles.css` | 修改 | 统一表单 gap 间距、login-overlay !important、协议 z-index 10000 |
| `wechat/admin/index.html` | **新增** | 管理后台完整单页应用（登录 + 仪表盘 + 用户管理） |
| `worker/index.js` | 修改 | 注册移除 phone 参数、忘记密码改为"账号不存在"、**新增 5 个管理后台 API 端点**、新增密保列自动迁移 |
| `vercel.json` | 修改 | 新增 `/admin/` 路由规则 |
| `wechat/PRD.md` | 更新 | 记录 v2.1 完整变更日志 + 管理后台文档

---

### 内测 2.0（2026-06-08 ~ 2026-06-09）

> ⚠️ **内测阶段说明**：内测 2.0 为内部测试版本，已部署到 CloudBase 静态托管（Web 版）和微信小程序（体验版）。关键变更为架构升级与安全保障。

#### 🆕 新增功能

- [x] **登录系统**：用户进入即需登录（微信/QQ/手机号/苹果账号），采用航旅纵横风格 UI
- [x] **用户协议**：登录前必须勾选同意《轻印用户协议》和《轻印隐私声明》
- [x] **足迹报告**：新增专属足迹报告页面，航旅纵横飞行轨迹风格设计（飞行路线可视化 + 时间线 + 城市排行 + 省份统计）
- [x] **打卡勋章弹窗**：打卡解锁省份勋章时，弹出光晕粒子动画提醒弹窗
- [x] **登录拦截**：所有功能页面（日志/打卡/分享/我的/足迹报告/设置）均需登录后才能访问
- [x] **全局更名**：产品正式命名为"轻印"（原 Mapory），所有界面、文档、配置统一更名

#### 🔧 功能改进

- [x] **打卡页全屏地图**：地图不再被框成正方形，改为铺满整个手机屏幕（自适应），支持滑动操作
- [x] **打卡点列表**：仅在需要时展开，不遮挡地图视野
- [x] **足迹报告交互**：修复"足迹报告"入口点击无反应的问题
- [x] **足迹报告融入打卡页**：在打卡页右侧工具栏新增「📊 报告」按钮，点击后底部弹出足迹报告面板（参考高德/百度地图设计）
  - 总览 Tab：核心数据统计 + 34省点亮网格 + 旅行偏好分析（最爱城市/最活跃月/最爱出行方式）
  - 城市排行 Tab：到访城市排序 + 柱状图可视化 + TOP1-3 金银铜徽标
  - 省份分布 Tab：34省份卡片网格，已点亮省份高亮显示
  - 旅行路线 Tab：打卡点间飞行路线展示，点击可聚焦到地图对应位置
  - Web 端同步实现：打卡列表页增加可展开的足迹报告区域

#### 🐛 Bug 修复

- [x] 修复 `bindPhone` 写入旧存储键 `mapory_login_info` → 改为 `qingyin_login_info`
- [x] 修复足迹报告页面缺少 `requireLogin()` 登录拦截
- [x] 修复设置页面缺少 `requireLogin()` 登录拦截
- [x] **我的页面勋章重复**：删除小程序「我的」页面中部重复的"我的勋章"卡片（与菜单项重复），同步清理 WXSS 样式
- [x] **我的页面顶部数据不匹配**：修复「段旅程、个打卡」区域照片数统计逻辑，统计范围扩展为「打卡照片 + 旅程封面 + 日记照片」，确保与下方详情一致
- [x] **打卡页/日志页数据不一致**：修复打卡页自动关联默认旅程的 bug（`journeyId` 不再自动取全局第一条旅程），确保未指定旅程的打卡不会错误关联；修复日记页面的照片统计同步问题

#### 🔗 域名与部署

- [x] **Web 端已部署**：`https://ai-native-d5gv1bzqle900971e-1439954016.tcloudbaseapp.com/`
- [x] **后台管理系统已上线**：`https://ai-native-d5gv1bzqle900971e-1439954016.tcloudbaseapp.com/#/admin`
- [ ] **自定义域名绑定**：`qingyinapp.cn`（DNS: 阿里云万网），待 SSL 证书签发后绑定到 CloudBase 静态托管
  - 环境 ID：`ai-native-d5gv1bzqle900971e`
  - 托管域名：`ai-native-d5gv1bzqle900971e-1439954016.tcloudbaseapp.com`
  - 当前状态：⚠️ 等待用户提供 SSL 证书 ID

#### 🆕 后台管理系统 v2

- [x] **管理员登录**：独立管理员密码认证，24小时有效管理 Token
- [x] **仪表盘**：总用户数 / 今日新增 / 本周新增 / 本月新增统计卡片 + 近7天注册趋势柱状图 + 最近注册用户列表
- [x] **用户管理**：全部用户列表（分页）、用户名搜索、单个用户删除（含二次确认）、一键清空全部用户数据（含二次确认）
- [x] **Worker API 扩展**：新增 5 个管理端点（`POST /api/admin/login`、`GET /api/admin/users`、`GET /api/admin/stats`、`DELETE /api/admin/users`、`DELETE /api/admin/clear-all`）
- [x] **部署地址**：`/admin/` 路径（Vercel + CloudBase 双平台同步部署）
- [x] **UI 设计**：暗色侧边栏 + 亮色内容区，桌面端优先，移动端适配汉堡菜单

#### 📝 文档变更

- [x] 产品命名统一：所有 MD 文档、配置文件中的 Mapory → 轻印/qingyin
- [x] 项目配置文件 `project.config.json` 项目名更新
- [x] 新增本文档版本记录机制，后续版本变更均在此记录

---

### 内测 1.0（2026-06-03 ~ 2026-06-07）

- [x] 产品命名：Mapory（后更名为轻印）
- [x] 旅程 CRUD 与日记管理
- [x] 地图打卡与足迹记录
- [x] 小票风格分享模板
- [x] 勋章系统（34 省市区）
- [x] 旅行人格判断
- [x] 数据洞察（月度活跃/足迹增长/季节分布/标签统计）
- [x] 权限管理系统（通知/位置/照片/相机）
- [x] 图片拍摄/选取统一入口
- [x] 行程回收站
- [x] 开发者反馈通道
- [x] 双端上线：CloudBase Web 版 + 微信小程序

---

### 内测 3.0（规划中）

- [ ] 卡片风格分享模板
- [ ] 时间线风格分享模板
- [ ] 杂志风分享模板
- [ ] 云端数据同步增强
- [ ] 多人协作旅行日记

---

## 8. 附录

### 8.1 项目文件结构

```
wechat/
├── index.html              # 主页面（单页应用入口）
├── script.js               # 核心交互逻辑
├── styles.css              # 全局样式表
├── china_provinces.json    # 中国省份地理数据
├── project.config.json     # 微信小程序项目配置
├── project.private.config.json  # 私有配置
├── admin/                  # 管理后台
│   └── index.html          # 管理后台单页应用
└── PRD.md                  # 本需求文档
worker/
├── index.js                # Cloudflare Workers API 主入口
├── schema.sql              # D1 数据库初始化 SQL
└── wrangler.toml           # Wrangler CLI 配置
```

### 8.2 关键依赖

- **Leaflet** `v1.9.4`：开源地图库
- **html2canvas** `v1.4.1`：DOM 截图导出
- **天地图瓦片服务**：中国地图底图

### 8.3 设计理念

轻印的设计哲学来源于对旅行记录痛点的洞察：

> "我们曾无数次在旅途中打开各种旅游日记、打卡工具，却总被复杂的功能劝退——明明只是想记下'今天去了哪里、看到了什么'，为什么需要填这么多表单？选分类、打标签、写攻略……这些真的有必要吗？"

因此轻印坚持以下原则：

1. **减法优先**：砍掉不必要的功能，只保留核心
2. **地图原生**：以地理坐标组织记忆，而非文件夹
3. **轻量体验**：无需注册、无需同步、打开即用
4. **权限透明**：所有敏感权限均主动告知用户用途
